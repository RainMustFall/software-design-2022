namespace Roguelike.Controllers.Misc;

// Small container that contains important controllers.
// It's only purpose is to improve readability
public record ControllerContainer(MapController MapController);